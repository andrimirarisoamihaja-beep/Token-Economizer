name: Test Automatique Python avec Groq IA

on: [push, pull_request]

jobs:
  verification-code:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code source
        uses: actions/checkout@v4

      - name: Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installer les outils (mypy, pytest, groq)
        run: pip install mypy pytest groq

      - name: Exécuter la vérification avec Groq IA
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python verifier_ia.py